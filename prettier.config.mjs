import config from '@mediaryorg/core/prettier'

/**
 * The shared house style, unchanged. Generated files are excluded in
 * .prettierignore — their formatting belongs to the generator that wrote them.
 *
 * @type {import('prettier').Config}
 */
export default config
